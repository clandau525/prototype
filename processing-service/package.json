{
  "name": "file-processing-service",
  "version": "1.0.0",
  "description": "File processing service for validation and security scanning",
  "main": "main.py",
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8001",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8001",
    "test": "pytest",
    "lint": "flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics",
    "format": "black . && isort ."
  },
  "dependencies": {},
  "devDependencies": {}
}